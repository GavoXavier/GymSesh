// import { Meteor } from 'meteor/meteor';
// import '../imports/api/FormDataCollection';
// import '../imports/api/methods';

// Meteor.startup(() => {
//   // code to run on server at startup
// });


import { Meteor } from 'meteor/meteor';
import '../imports/api/GymSessionsCollection';
import '../imports/api/methods';

Meteor.startup(() => {
  // code to run on server at startup
});
